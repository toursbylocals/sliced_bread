/* eslint-disable camelcase */
import { League_Spartan, Roboto } from 'next/font/google';

export const leagureSpartan = League_Spartan({
  subsets: ['latin'],
  weight: ['600', '700'],
});

export const roboto = Roboto({
  subsets: ['latin'],
  weight: '400',
});
